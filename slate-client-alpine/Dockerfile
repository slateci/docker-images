# syntax=docker/dockerfile:1.4
FROM alpine:3.16.0

# Package installs/updates:
RUN apk add --no-cache \
    boost-dev \
    cmake \
    curl-dev \
    gcc \
    g++ \
    libgcc \
    libstdc++ \
    make \
    musl-dev \
    nghttp2 \
    openssl-dev \
    zlib-dev
